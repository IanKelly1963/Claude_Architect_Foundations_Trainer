{
  "d1-1.1-b|B": "Tool results must be appended to the assistant message that requested them, rather than being sent back as a separate new message.",
  "d1-1.1-c|D": "Fixed pipelines are incompatible with MCP tools, which require dynamic discovery and cannot be bound to a predetermined call order.",
  "d1-1.1-e|D": "Prompt caching was disabled by the change, and the cache is the mechanism that preserves conversational memory across requests to the API.",
  "d1-1.1-f|C": "Appending each `tool_result` block to the conversation history before issuing the next request.",
  "d1-1.1-g|D": "Set the cap to exactly the number of tools the agent has available, since a well-behaved agent should need to call each of them only once.",
  "d1-1.2-b|D": "The coordinator would no longer receive `stop_reason` values from the delegated calls, so it could not tell when the subagent had actually finished.",
  "d1-1.2-d|B": "Deduplicate the merged result set by URL before the combined findings are passed to the synthesis agent for report generation.",
  "d1-1.2-e|B": "Instruct the synthesis agent to expand thin sections using its own background knowledge, so every subtopic reaches comparable depth and length in the final report.",
  "d1-1.2-f|C": "Add a preprocessing classifier that routes the message to whichever single specialist queue best matches its dominant topic, discarding the secondary concerns.",
  "d1-1.3-b|C": "Give the synthesis subagent direct access to the web-search tool so that it can look up citations for its own claims while it is combining the findings it receives.",
  "d1-1.3-d|B": "Coordinator prompts must never name specific tools, because tool availability varies between subagent definitions and can change between runs.",
  "d1-1.3-g|B": "The subagent's memory was evicted under context pressure; raise its context limit so that state survives between separate invocations.",
  "d1-1.4-c|C": "A validation note in the `process_refund` tool description that explains the $500 limit and the policy rationale behind it.",
  "d1-1.4-f|C": "The gate should be removed and replaced with a prompt instruction, since a hard block confuses the model and produces unhelpful customer-facing errors.",
  "d1-1.4-g|B": "A CLAUDE.md instruction stating that the security scan must always be consulted before any review comment is posted to the pull request.",
  "d1-1.5-a|C": "A `PreToolUse` hook that rewrites each tool's input arguments so that every backend is asked for a consistent date format.",
  "d1-1.5-b|C": "`Stop`, which runs at the end of the turn and can roll back any tool effects that have already occurred.",
  "d1-1.5-d|B": "400 consecutive passing cases is statistically sufficient evidence of compliance, so the existing system prompt is adequate as written.",
  "d1-1.5-e|D": "Deciding whether the agent should be permitted to call a particular tool before the call executes.",
  "d1-1.6-a|D": "Run three independent review passes over the full pull request, and only report issues that appear in at least two of the three separate runs.",
  "d1-1.6-b|B": "A fixed pipeline: enumerate every source file in the repository, then generate exactly one test file per source file, working in alphabetical order.",
  "d1-1.6-c|D": "Researching an open-ended topic where the relevant subdomains cannot be known before the research begins.",
  "d1-1.6-e|D": "The topic string was too short, and a longer, more explicit topic description would have produced a better decomposition from the coordinator.",
  "d1-1.6-f|D": "Missed cross-file bugs, which occur because the related files cannot all be held in context simultaneously.",
  "d1-1.7-b|B": "Resume the old session, because session resumption always preserves more useful context than starting fresh and re-injecting a written summary of it.",
  "d1-1.7-e|C": "Tool results older than 24 hours are automatically discarded from the session, so the resumed context comes back silently empty.",
  "d1-1.7-f|D": "Raise the context window size so that a single session can span the entire multi-day investigation without truncation.",
  "d1-1.7-g|C": "The prior session ended cleanly yesterday afternoon, and no code in the repository has changed since."
}
